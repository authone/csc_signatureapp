extends layout

block content

  script.
    var onInfo = function() {
      console.log('=== onInfo ===');
      $.ajax({
          url: '/csc/info', 
          type: 'GET',
          success: function(data) {
            window.location.href = "/";
          },
          //- error: function(error) {
          //-   alert( "Request failed: " + error );
          //- },
          //- complete: function () {
          //-   console.log("/csc/info finished execution");
          //- },
      });
    };

  h1= title
  p 
    button(type="button", onclick='onInfo()') Get Server Info
  
  if cscInfoLogo
    img.image(src= '#{cscInfoLogo}', height=64)
  
  pre #{cscInfo}
                
